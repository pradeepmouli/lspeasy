---
name: lspeasy
description: "lspeasy monorepo — 3 packages. client: Connect to LSP servers with typed client API; core: Core types, transports, and utilities for LSP SDK; server: Build LSP language servers with a simple, fully-typed API"
license: MIT
---
# lspeasy

## Which package?

- **client** — Connect to LSP servers with typed client API Use when: You are implementing a custom client layer and need the same validation behav...
- **core** — Core types, transports, and utilities for LSP SDK Use when: You are building a browser-based LSP client, a WebSocket-backed language serv...
- **server** — Build LSP language servers with a simple, fully-typed API Use when: The client sets `partialResultToken` in the request params and you want to st...