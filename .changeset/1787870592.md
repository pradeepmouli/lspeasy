---
  "@lsproxy/cli": patch
---

- chore(deps): update dependencies (pnpm update -r --latest)
- fix(cli): wire up npx invocation mode in generated skill
- test(e2e): add coverage for TypeScript's native tsc --lsp server
