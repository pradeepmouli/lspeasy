---
"@lsproxy/cli": minor
---

Add a version command: `lsproxy --version` / `-V` / `lsproxy version` print the CLI version and exit (previously these printed the tagline). Sourced from `package.json`.
